@model HomeViewModel

@{
    ViewData["Title"] = "Home";
    ViewBag.FromHome = true;
}

@foreach (var category in @Model!.Categories)
{
    <partial name="../Partials/Category.cshtml" model="@category" />
}
<partial name="../Partials/CreateCategoryForm.cshtml" model="@Model.CreateCategoryModel" />
