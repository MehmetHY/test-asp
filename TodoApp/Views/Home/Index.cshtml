@model HomeViewModel

@{
    ViewData["Title"] = "Home";
}

@if (@Model != null)
{
    
    @foreach (var category in @Model.Categories)
    {
        <partial name="Partials/Category.cshtml" model="@category" />
    }

}